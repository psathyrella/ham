language: python

before_install:
  - sudo apt-get update -qq
  - sudo apt-get install -y build-essential libboost-graph-parallel-dev libboost-mpi-dev libboost-mpi-python-dev libboost-all-dev
  - sudo apt-get install -f -y scons
  - sudo apt-get autoremove -y
  - sudo apt-get autoclean
  - git config --global user.name "Erick Matsen"
  - git config --global user.email "matsen@fhcrc.org"

cache: true

install:
  - scons

notifications:
  email:
    - matsen@fhcrc.org

